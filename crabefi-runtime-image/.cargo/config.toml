# `xtask::runtime` owns build-std and target codegen flags because it must also
# add the architecture-specific map path. Keeping this file free of build
# options avoids affecting host-side tests and avoids a second configuration
# silently overridden by the xtask command line and RUSTFLAGS.
