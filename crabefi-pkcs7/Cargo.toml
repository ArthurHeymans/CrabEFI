[package]
name = "crabefi-pkcs7"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Allocation-free DER, X.509, PKCS#7 SignedData and RSA PKCS#1 v1.5 SHA-256 verification"

[dependencies]
sha2 = { version = "0.11", default-features = false }
