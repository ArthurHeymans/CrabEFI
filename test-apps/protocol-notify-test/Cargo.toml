[package]
name = "protocol-notify-test"
version = "0.1.0"
edition = "2021"
description = "Test EFI application for RegisterProtocolNotify and LocateHandle(ByRegisterNotify)"

[workspace]

[dependencies]
r-efi = "5.3"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"

[profile.dev]
panic = "abort"
